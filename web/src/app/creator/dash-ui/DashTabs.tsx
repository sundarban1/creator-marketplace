/**
 * Creator-dashboard tab bar — re-exports the site's editorial `ui/Tabs` so
 * applications/work/discover use the same single-violet underline as the
 * rest of the app.
 */
export { Tabs as DashTabs } from '../../ui/Tabs';
export type { TabItem as DashTabItem } from '../../ui/Tabs';
