/**
 * Page title block for the creator dashboard — re-exports the site's
 * `ui/PageHeader` (admin's plain bold-sans header) so Creator and Business
 * read as one system.
 */
export { PageHeader as DashPageHeader } from '../../ui/PageHeader';
