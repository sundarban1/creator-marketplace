/**
 * Creator-dashboard card — re-exports the site's editorial `ui/Card` so the
 * dashboard/wallet/settings/profile/referrals surfaces share the same
 * border/hover-lift/gradient-hairline treatment as the rest of the app.
 */
export { Card as DashCard, CardHeader as DashCardHeader } from '../../ui/Card';
