/**
 * Dashboard metric tile — re-exports the site's `ui/StatCard` (admin's
 * icon-tile + stacked value/label layout) so Creator and Business read as
 * one system.
 */
export { StatCard as DashStatCard } from '../../ui/StatCard';
