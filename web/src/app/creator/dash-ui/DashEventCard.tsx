/**
 * Creator-dashboard event card — re-exports the site's editorial
 * `public/EventCard` so the dashboard's "Recommended opportunities" rail and
 * the dashboard discover view use the same bordered/hover-lift treatment as
 * the public marketplace browse pages, instead of a separate flat style.
 */
export { EventCard as DashEventCard } from '../../public/EventCard';
